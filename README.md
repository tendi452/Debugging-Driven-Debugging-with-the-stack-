# Debugging-Driven-Debugging-with-the-stack-
fixing our code
 steps on how to fix our code
 # step 1: Make observations
 # STEP 2: Question
 # step 3: Hypothesis 
 # step 4: Make a prediction
 # step 5: Test your hypothesis 

 example: 
 # Function 
 def  counting_colour(colours): [  

     count_red = 0 
     count_green = 0
     count_blue = 0 

     colour_split = colours_split(",")

# logical programming 

    if colour == "red":
    count_red += 1

    elif colour == "blue"
         count_red += 1

   elif colour == "green" 
       count_green = += 1


return f"Red Count" = {count-red}

print(counting_colour("blue, green, red, blue, green, blue, red, red))
Red count = 0, green count = 0, "blue count = 1"

Correction 
# sanity check, our code guves bue count = 1, our split is incorrect.
# the space between our quotes is causing problems 
solution 
colour_split =colours(", ")
Red count = 3, green count = 2, Blue coumt = 4
# we have fixed our code!
